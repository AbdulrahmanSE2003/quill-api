info:
  name: Get Book Rating
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/ratings/6a6d2e8f5cbbb2c9192c136f"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
