info:
  name: Delete Me
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{URL}}/users/me"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
