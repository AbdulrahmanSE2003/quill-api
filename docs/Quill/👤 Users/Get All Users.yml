info:
  name: Get All Users
  type: http
  seq: 4

http:
  method: GET
  url: "{{URL}}/users"
  auth:
    type: bearer
    token: "{{ADMIN_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
