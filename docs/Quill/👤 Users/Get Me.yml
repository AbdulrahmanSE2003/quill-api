info:
  name: Get Me
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/users/me"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
