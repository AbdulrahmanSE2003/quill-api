info:
  name: Get All Books
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/books"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
