info:
  name: Get Book
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/books/6a6d2e8f5cbbb2c9192c136f"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
