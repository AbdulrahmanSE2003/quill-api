info:
  name: Add Bookmark
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/bookmarks/6a6d2e8f5cbbb2c9192c136f"
  body:
    type: json
    data: |-
      {
        "chunkIndex": 7
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
