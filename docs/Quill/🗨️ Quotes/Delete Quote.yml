info:
  name: Delete Quote
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL}}/quotes/6a6d2e8f5cbbb2c9192c136f"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
