info:
  name: Add to wishlist
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/wishlist/6a6d2e8f5cbbb2c9192c136f"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
