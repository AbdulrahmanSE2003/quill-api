info:
  name: Get my wishlist
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/wishlist/"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
